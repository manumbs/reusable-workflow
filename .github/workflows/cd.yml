name: CD

on: 
  workflow_call:
    inputs:
      username:
        required: true
        type: string

jobs:
  cd:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: echo This is CD!
      
      - run: echo ${{ inputs.username }}
